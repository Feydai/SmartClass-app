import React from 'react';
import { View, Text } from 'react-native';
// import { ScreenEquipementProps } from '../../types/screenProps';

// type Props = ScreenEquipementProps;
// const ScreenEquipement: React.FC<Props> = ({ /* props */ }) => {
const ScreenEquipement = (): React.JSX.Element => {
  return (
    <View>
      <Text>ScreenEquipement</Text>
    </View>
  );
};

export default ScreenEquipement;